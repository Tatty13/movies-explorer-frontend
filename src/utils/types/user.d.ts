export type User = {
  name: string;
  email: string;
};

export type LoginData = {
  name: string;
  password: string;
};

export type SignupData = {
  name: string;
  email: string;
  password: string;
};
